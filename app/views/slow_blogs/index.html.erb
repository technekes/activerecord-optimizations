<p id="notice"><%= notice %></p>

<h1>Listing Blogs</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th># of Comments</th>
      <th>Last Comment</th>
    </tr>
  </thead>

  <tbody>
    <% @blogs.each do |blog| %>
      <tr>
        <td><%= link_to blog.title, slow_blog_path(blog) %></td>
        <td><%= blog.comments.count %></td>
        <td><%= blog.comments.last.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Blog', new_slow_blog_path %>
